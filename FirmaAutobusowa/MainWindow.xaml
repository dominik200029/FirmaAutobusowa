<Window x:Class="FirmaAutobusowa.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Firma autobusowa" Height="456" Width="830">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="MainPanel"
            Grid.Row="0"
            Orientation="Horizontal"
            VerticalAlignment="Center"
            Margin="0,0,0,10"
           >

            <ComboBox x:Name="Table"
              Width="150"
              SelectionChanged="TableComboBox_SelectionChanged"/>

            <Button Content="Dodaj"
            Width="75"
            Click="AddItemButton_Click"/>

            <Button Content="Usuń"
            Width="75"
            Click="DeleteItemButton_Click"/>

        </StackPanel>



        <!-- do tego panela dodaje Boxy jako formularz -->
        <StackPanel x:Name="FormPanel"
            Grid.Row="1"
            Orientation="Horizontal"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Margin="0,0,0,10">
        </StackPanel>

        <DataGrid x:Name="TableGrid"
              Grid.Row="2"
              AutoGenerateColumns="True"
              Margin="0,0,0,10"
              IsReadOnly="True"/>
    </Grid>
</Window>
